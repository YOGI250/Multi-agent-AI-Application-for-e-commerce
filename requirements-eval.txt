# ==================================================
# EVAL-ONLY DEPENDENCIES
# Install with: pip install -r requirements-eval.txt
# These are NOT included in the Docker image (conflict with langfuse grpcio)
# ==================================================
deepeval==4.0.5
